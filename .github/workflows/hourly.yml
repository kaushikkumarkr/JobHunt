# name: Hourly-US-Tech-Job-Finder

# on:
#   schedule:
#     - cron: "17 * * * *"  # Run at minute 17 to avoid top-of-hour congestion
#   workflow_dispatch:      # Allow manual trigger

# jobs:
#   run-finder:
#     runs-on: ubuntu-latest
    
#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v3

#     - name: Set up Python
#       uses: actions/setup-python@v4
#       with:
#         python-version: '3.11'

#     - name: Install dependencies
#       run: |
#         python -m pip install --upgrade pip
#         pip install -r requirements.txt
#         playwright install chromium

#     - name: Run Job Finder
#       env:
#         # Core Secrets
#         GOOGLE_SEARCH_API_KEY: ${{ secrets.GOOGLE_SEARCH_API_KEY }}
#         GOOGLE_SEARCH_ENGINE_ID: ${{ secrets.GOOGLE_SEARCH_ENGINE_ID }}
#         SHEET_ID: ${{ secrets.SHEET_ID }}
        
#         # Google Auth (Base64 Encoded service_account.json)
#         GOOGLE_APPLICATION_CREDENTIALS_JSON_BASE64: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS_JSON_BASE64 }}
        
#         # Optional: If using Gmail User OAuth flow needing a token
#         GMAIL_TOKEN_JSON: ${{ secrets.GMAIL_TOKEN_JSON }}
        
#         # LLM Keys
#         GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
#         OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
#         HUGGINGFACE_API_KEY: ${{ secrets.HUGGINGFACE_API_KEY }}

#         # Notifications
#         TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
#         TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
#         DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
#         EMAIL_APP_PASSWORD: ${{ secrets.EMAIL_APP_PASSWORD }}
        
#       run: |
#         python runner.py
